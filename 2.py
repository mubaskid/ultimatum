def main():
    fileVariable = open("array allocation", "w")
    fileVariable.write(
        "Memory allocation is the process",)
    fileVariable.write("by which a program is assigned or allocated",)
    fileVariable.write(
        "to a particular empty block of space in computer memory.")

    print(fileVariable.write)


main()
